@page
@model AtoZClinical.Web.Pages.Account.ResendConfirmationModel
@{
    ViewData["Title"] = "Resend Confirmation";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resend Confirmation - A to Z Clinical</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/clinical.css" asp-append-version="true" />
</head>
<body>
    <div class="login-panel atz-card p-4">
        <div class="text-center mb-4">
            <h3 class="fw-bold" style="color:#0b4f8a">Resend confirmation email</h3>
            <p class="text-muted mb-0">Enter your username or email</p>
        </div>
        @if (Model.Submitted)
        {
            <div class="alert alert-info">
                If an account with that username or email exists and is not yet confirmed, a confirmation link has been sent.
            </div>
            <a class="btn btn-atz-primary w-100" asp-page="/Account/Login">Back to Login</a>
        }
        else if (Model.EmailDeliveryFailed)
        {
            <div class="alert alert-danger">
                We could not send the confirmation email. Please try again later.
            </div>
            <button type="button" class="btn btn-atz-primary w-100 mb-2" onclick="location.reload()">Try Again</button>
            <a class="btn btn-outline-secondary w-100" asp-page="/Account/Login">Back to Login</a>
        }
        else
        {
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <div class="mb-3">
                    <label asp-for="Input.Username" class="form-label"></label>
                    <input asp-for="Input.Username" class="form-control" autocomplete="username" />
                    <span asp-validation-for="Input.Username" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-atz-primary w-100 mb-2">Send confirmation link</button>
                <a class="btn btn-outline-secondary w-100" asp-page="/Account/Login">Cancel</a>
            </form>
        }
    </div>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>
